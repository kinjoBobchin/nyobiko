{
  "rules": {
    "no-extra-semi": "warn",
    "quotes": ["warn", "double"],
    "space-before-blocks": ["warn",
      {
        "functions": "always",
        "keywords": "never",
        "classes": "never"
      }
    ]
  }
}